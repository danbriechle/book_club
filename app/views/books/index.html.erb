<% @books.each do |book| %>
<h1><%= link_to book.title, book_path(book) %></h1>
<p><%= link_to("Author: #{book.author.name}", author_path(book.author)) %> </p>
<p> Length: <%= book.pages %> </p>
<% end %>
